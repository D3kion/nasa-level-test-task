const BitArray = require('./BitArray');

class Decoder {
  // BitArray -> array of numbers
  Gamma(bitArray) {
    return [];
  }

  // BitArray -> array of numbers
  Delta(bitArray) {
    return [];
  }
}

module.exports = Decoder;
