const BitArray = require('./BitArray');

class Encoder {
  // array of numbers -> BitArray
  Gamma(x) {
    return new BitArray();
  }

  // array of numbers -> BitArray
  Delta(x) {
    return new BitArray();
  }
}

module.exports = Encoder;
